# ai-security
AI Security course content

* `ssh`: Use a docker container ssh server to locally test using a private/public key to log into it
* `attestation`: Different ways to verify an AI model file
